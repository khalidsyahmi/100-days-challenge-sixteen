<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: 'update' }) %>
    <link rel="stylesheet" href="/styles/forms.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <main>
      <h1>Update post</h1>
      <!-- // action is important to send post request in routes -->
      <form action="/posts/<%= updateKey._id %>/edit" method="POST">
        <div class="form-control">
          <label for="title">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            required
            value="<%= updateKey.title %>"
          />
        </div>
        <div class="form-control">
          <label for="summary">Summary</label>
          <input
            type="text"
            id="summary"
            name="summary"
            required
            maxlength="255"
            value="<%= updateKey.summary %>"
          />
        </div>
        <div class="form-control">
          <label for="content">Post Content</label>
          <textarea id="content" name="content" required rows="5">
<%= updateKey.body %></textarea
          >
        </div>
        <button class="btn">Update Post</button>
      </form>
    </main>
  </body>
</html>
